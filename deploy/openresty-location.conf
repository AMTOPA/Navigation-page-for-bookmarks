# Replace the current auth_basic/static root behavior after the Docker service is healthy.
# Keep the existing TLS certificate, logs and HTTP-to-HTTPS redirect directives.
location / {
    proxy_pass http://127.0.0.1:8765;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_http_version 1.1;
    client_max_body_size 110m;
    proxy_read_timeout 300s;
}

# Add application/json to the existing http-level gzip_types directive:
# gzip_types text/plain text/css application/javascript application/json
#            application/problem+json application/xml image/svg+xml;
